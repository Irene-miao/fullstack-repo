import React from 'react';

const Total = (props) => {
    return (
        <div>
            <p>There are a total of {props.number} exercises.</p>
        </div>
    )
}

export default Total;
